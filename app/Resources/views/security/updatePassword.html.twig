{% extends 'base.html.twig' %}

{% block body %}
<div class="container pt-5 pb-5" align="center">
    <div class="card">
        <h1 class="card-header bg-green">Modifier votre mot de passe</h1>
        <div class="card-body pt-5 pb-5">
            <div class="row">
                <div class="col-md-6 offset-md-3 text-center mb-3">
                    {{ form_start(form) }}
                    {{ form_widget(form.password) }}
                    {{ form_widget(form.password) }}
                    <div class="text-center">
                        <button type="submit" class="btn btn-dark mt-4 bg-green">Modifier</button>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
