{% extends 'baseAdmin.html.twig' %}

{% block titleDashboard %}
    <h2>Ajouter une galerie d'images</h2>
{% endblock %}
{% block body %}
    <div class="container pt-5 pb-5">
        <div class="row">
            <div class="col-sm-6 offset-sm-3">
                <div class="card">
                    {{ form_start(form) }}
                    <div class="card-body">
                        <h5 class="card-title">Créer votre dossier d'images</h5>
                        <p class="card-text">{{ form_start(form) }}</p>
                        <input class="btn btn-dark" type="submit" value="Ajouter">
                        <p class="card-text">{{ form_end(form) }}</p>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}



