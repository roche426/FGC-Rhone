{% extends 'baseAdmin.html.twig' %}

{% block titleDashboard %}
    <h2>Liste des membres du site</h2>
{% endblock %}

{% block body %}
        <table class="table" align="center">
            <thead class="thead-dark ">
            <tr>
                <th scope="col" class="text-center">Prénom</th>
                <th scope="col" class="text-center">Nom</th>
                <th scope="col" class="text-center">Date de naissance</th>
                <th scope="col" class="text-center">Email</th>
                <th scope="col" class="text-center">Téléphone</th>
                <th scope="col" class="text-center">Adresse</th>
                <th scope="col" class="text-center">Ville</th>
                <th scope="col" class="text-center">Inscription le</th>
                <th scope="col" class="text-center">Action</th>
            </tr>
            </thead>
            <tbody>
        {% for user in users %}
            <tr>
                <td class="text-center">{{ user.firstName }}</td>
                <td class="text-center">{{ user.lastName }}</td>
                <td class="text-center">{{ user.birthDate |date('d/m/Y')}} </td>
                <td class="text-center">{{ user.email }}</td>
                <td class="text-center">{{ user.phoneNumber }}</td>
                <td class="text-center">{{ user.address }} {{ user.AdditionalAddress }}</td>
                <td class="text-center">{{ user.postalCode }} {{ user.city }}</td>
                <td class="text-center">{{ user.registrationDate |date('d/m/Y')}} </td>
                <td class="text-center">
                    <a class="btn btn-info" href="{{ path('admin_show_user', {id:user.id}) }}">Voir</a>
                    <a class="btn btn-danger alert-delete" href="{{ path('delete_user', {id:user.id}) }}"
                        data-confirm-message="Confirmer la suppression de l'utilisateur?">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
            </tbody>
        </table>
        <a class="btn btn-primary mb-5" href="{{ path('registration') }}">Ajouter un membre</a>
        <a class="btn btn-warning mb-5" href="{{ path('admin_users_inactives') }}">Liste membres inactifs</a>
{% endblock %}
