{% extends 'baseAdmin.html.twig' %}

{% block titleDashboard %}
    <h2>Liste des documents partagés</h2>
{% endblock %}

{% block body %}
        <table class="table">
            <thead class="thead-dark" >
            <tr>
                <th scope="col" class="text-center">Nom du fichier</th>
                <th scope="col" class="text-center">Droit d'accès</th>
                <th scope="col" class="text-center">Document en cours de partage?</th>
                <th scope="col" class="text-center">Date de téléchargement</th>
                <th scope="col" class="text-center">Action</th>
            </tr>
            </thead>
            <tbody>
        {% for file in files %}
            <tr>
                <td class="text-center">{{ file.nameFile }}</td>
                <td class="text-center">
                    {% if file.fileAccess == constant('MEMBERS_ACCESS_FILE', file)%}
                        Membres
                    {% elseif file.fileAccess == constant('BUREAU_MEMBERS_ACCESS_FILE', file)%}
                        Membres du bureau
                    {% elseif file.fileAccess == constant('ADMIN_ACCESS_FILE', file)%}
                        Administrateurs
                    {% else %}
                        Public
                    {% endif %}
                </td>
                <td class="text-center">
                    {% if file.isShared %}
                        Oui
                    {% else %}
                        Non
                    {% endif %}
                </td>
                <td class="text-center">{{ file.dateUpload |date('d/m/Y') }} </td>

                <td class="text-center">
                    <a class="btn btn-info" href="{{ path('admin_download_files', {id:file.id}) }}">Voir</a>
                    <a class="btn btn-danger alert-delete" href="{{ path('admin_delete_files', {id:file.id}) }}"
                       data-confirm-message="Confirmer la suppression du document?">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
            </tbody>
        </table>
        <a class="btn btn-primary mb-5" href="{{ path('admin_add_files') }}">Ajouter un document</a>
{% endblock %}
