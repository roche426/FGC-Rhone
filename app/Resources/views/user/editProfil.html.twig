{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>Mes informations</h1>
        </div>
        <div class="row">
            {{ form_start(editForm) }}
        </div>
        <div class="row">
            <input class="btn btn-dark" type="submit" value="modifier">
        </div>
        {{ form_end(editForm) }}

        <a class="btn btn-danger" href="{{ path('delete_user', {role:'user', id:app.user.id}) }}">Supprimer</a>
        <a class="btn btn-warning" href="{{ path('disable_user', {role:'user', id:app.user.id}) }}">Désactiver</a>

    </div>



{% endblock %}


